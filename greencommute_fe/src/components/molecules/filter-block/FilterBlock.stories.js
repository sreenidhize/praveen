import FilterBlock from './FilterBlock';

export default {
    title: 'molecules/FilterBlock',
    component: FilterBlock
}

const Template = (args) => <FilterBlock {...args} />

export const Primary = Template.bind({});

Primary.args = {
   
}